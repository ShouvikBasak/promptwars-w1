# Walkthrough – Vertex AI Migration

Migrated AI features from the legacy Google Generative AI SDK to Vertex AI for production compatibility.

## Changes:
- Replaced `google-generativeai` with `google-cloud-aiplatform`.
- Configured IAM-based authentication (ADC).
- Verified structured JSON output with Gemini 1.5 Flash.
